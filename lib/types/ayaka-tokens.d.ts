/**
 * Ayaka theme token overrides for the DSH `--dsw-*` alias layer.
 *
 * Each token maps to a `{ light, dark }` pair so the theme never goes
 * illegible when the user switches color scheme. The palette is an
 * ice-bloom blue inspired by Kamisato Ayaka.
 */
import type { ThemeTokenOverrides } from '@deepseek-ai/dsh-client-ui-theme/client';
/** Ice-bloom token overrides: light palette is pale ice-blue, dark is deep night-frost. */
export declare const AYAKA_TOKENS: ThemeTokenOverrides;
//# sourceMappingURL=ayaka-tokens.d.ts.map